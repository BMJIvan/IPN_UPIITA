clc
% clear all
close all
a(:,:,1)=255*ones(100,100);
a(:,:,2)=0*ones(100,100);
a(:,:,3)=0*ones(100,100);

b(:,:,1)=0*ones(100,100);
b(:,:,2)=250*ones(100,100);
b(:,:,3)=0*ones(100,100);

c(:,:,1)=0*ones(100,100);
c(:,:,2)=0*ones(100,100);
c(:,:,3)=255*ones(100,100);

d(:,:,1)=255*ones(100,100);
d(:,:,2)=255*ones(100,100);
d(:,:,3)=0*ones(100,100);

e(:,:,1)=0*ones(100,100);
e(:,:,2)=255*ones(100,100);
e(:,:,3)=255*ones(100,100);

f(:,:,1)=255*ones(100,100);
f(:,:,2)=0*ones(100,100);
f(:,:,3)=255*ones(100,100);

g(:,:,1)=0*ones(100,100);
g(:,:,2)=0*ones(100,100);
g(:,:,3)=0*ones(100,100);

h(:,:,1)=255*ones(100,100);
h(:,:,2)=255*ones(100,100);
h(:,:,3)=255*ones(100,100);

im=[a,b,c,d;e,f,g,h];
imshow(uint8(im))
